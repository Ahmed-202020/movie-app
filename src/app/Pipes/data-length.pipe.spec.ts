import { DataLengthPipe } from './data-length.pipe';

describe('DataLengthPipe', () => {
  it('create an instance', () => {
    const pipe = new DataLengthPipe();
    expect(pipe).toBeTruthy();
  });
});
